class Algorithm:

    def __init__(self, heuristic, cost, start_puzzle, solved_puzzle, size)
        self.size = size
        self.start = start
        self.stop = stop
        self.open = 0
        self.closed = 0
        self.heuristic = heuristic
